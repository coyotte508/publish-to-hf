name: Publish to Hugging Face

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      id-token: write  # required so the job can request an OIDC token from GitHub
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install --upgrade "huggingface_hub>=1.16.1"

      - name: Exchange GitHub OIDC token for a Hub token
        run: |
          set -euo pipefail

          ID_TOKEN=$(curl -sSf \
            -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=https://huggingface.co" \
            | jq -r .value)

          # Decode the JWT payload (middle segment) and print non-sensitive claims
          # — useful for debugging "no trusted publisher matches" errors.
          PAYLOAD=$(echo "$ID_TOKEN" | cut -d. -f2)
          # Pad base64url to a multiple of 4 so `base64 -d` is happy.
          while [ $((${#PAYLOAD} % 4)) -ne 0 ]; do PAYLOAD="${PAYLOAD}="; done
          CLAIMS=$(echo "$PAYLOAD" | tr '_-' '/+' | base64 -d 2>/dev/null)
          echo "OIDC token claims:"
          echo "$CLAIMS" | jq .

          # Capture body, status, and response headers separately so we can
          # print the error (and x-request-id) on 4xx/5xx.
          HEADERS_FILE=$(mktemp)
          RESPONSE=$(curl -sS -D "$HEADERS_FILE" -w '\n%{http_code}' -X POST "https://huggingface.co/oauth/token" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg t "$ID_TOKEN" --arg r "coyotte508/published-from-github" \
                  '{
                    grant_type:         "urn:ietf:params:oauth:grant-type:token-exchange",
                    subject_token_type: "urn:ietf:params:oauth:token-type:id_token",
                    subject_token:      $t,
                    resource:           $r
                  }')")
          STATUS=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          echo "Response status: $STATUS"
          echo "Response headers:"
          # Lowercase header names so x-request-id is easy to spot regardless of casing.
          sed -E 's/^([A-Za-z0-9-]+):/\L\1:/' "$HEADERS_FILE"

          if [ "$STATUS" -ne 200 ]; then
            REQUEST_ID=$(grep -i '^x-request-id:' "$HEADERS_FILE" | head -n1 | sed 's/.*: *//' | tr -d '\r')
            echo "::error::Token exchange failed with HTTP $STATUS (x-request-id: ${REQUEST_ID:-unknown})"
            echo "$BODY" | jq . 2>/dev/null || echo "$BODY"
            exit 1
          fi

          HF_TOKEN=$(echo "$BODY" | jq -r .access_token)
          echo "::add-mask::$HF_TOKEN"
          echo "HF_TOKEN=$HF_TOKEN" >> "$GITHUB_ENV"

      - name: Upload model/ to the Hub
        # We can't use `hf upload` because it calls create_repo() first, which
        # requires permissions a Trusted Publisher token doesn't have.
        # Calling upload_folder() directly skips that step.
        run: |
          python - <<'PY'
          import os
          from huggingface_hub import upload_folder

          upload_folder(
              repo_id="coyotte508/published-from-github",
              folder_path="./model",
              commit_message=f"Publish from {os.environ['GITHUB_SHA'][:7]}",
          )
          PY
